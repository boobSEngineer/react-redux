let initialState = {
    usersDialogs: [
        {id: 1, name: 'Masha'},
        {id: 2, name: 'Sasha'},
        {id: 3, name: 'Dasha'},
        {id: 4, name: 'Ira'},
        {id: 5, name: 'Ko'},
    ],
}

const navbarReducer = (state = initialState, action) => {
    return(state);
}

export default navbarReducer;
